export const subTitle = (item) => {
    return {
        type: 'SUB_TITLE',
        item
    }
}

export const patientInfo = (item) => {
    return {
        type: 'PATIENT_INFO',
        item
    }
}